#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int suma=0;
    string d_tys;
    d_tys=to_string(pow(2,1000));
    for(int i=0;i<d_tys.size()-7;i++)
    {
        int liczba = d_tys[i] - '0';
        suma+=liczba;
    }
    cout << suma << endl;
    return 0;
}
